<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured logging. In prod / functional-equivalence (empty active profile) the app
  emits JSON with trace correlation (traceId/spanId via MDC when tracing is on). The
  benchmark profile drops the root level to WARN, so the measured request path emits no
  application logs and pays no logging cost.
-->
<configuration>
	<springProperty scope="context" name="appName" source="spring.application.name"/>

	<springProfile name="benchmark">
		<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
			<encoder>
				<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{0} - %msg%n</pattern>
			</encoder>
		</appender>
		<root level="WARN">
			<appender-ref ref="CONSOLE"/>
		</root>
	</springProfile>

	<springProfile name="!benchmark">
		<appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
			<encoder class="net.logstash.logback.encoder.LogstashEncoder">
				<includeMdcKeyName>traceId</includeMdcKeyName>
				<includeMdcKeyName>spanId</includeMdcKeyName>
				<customFields>{"service":"${appName}"}</customFields>
			</encoder>
		</appender>
		<root level="INFO">
			<appender-ref ref="JSON"/>
		</root>
	</springProfile>
</configuration>
